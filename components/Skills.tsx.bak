'use client'

import { motion } from 'framer-motion'
import { BarChart3, Database, Layout, TrendingUp, PieChart, Code2 } from 'lucide-react'

const skillCategories = [
  {
    title: 'Business & Functional Analysis',
    icon: Layout,
    skills: [
      { name: 'Requirements Gathering', level: 95 },
      { name: 'Business Process Mapping', level: 90 },
      { name: 'UAT Planning & Execution', level: 92 },
      { name: 'BRD/FSD Documentation', level: 88 },
      { name: 'Stakeholder Management', level: 90 },
    ],
  },
  {
    title: 'Data Analysis & EDA',
    icon: BarChart3,
    skills: [
      { name: 'Python (Pandas, NumPy)', level: 85 },
      { name: 'Exploratory Data Analysis', level: 88 },
      { name: 'SQL', level: 90 },
      { name: 'Statistical Analysis', level: 82 },
      { name: 'Data Validation', level: 90 },
    ],
  },
  {
    title: 'Data Visualization & Reporting',
    icon: PieChart,
    skills: [
      { name: 'Power BI', level: 90 },
      { name: 'Tableau', level: 85 },
      { name: 'Matplotlib', level: 80 },
      { name: 'Excel (Advanced)', level: 92 },
      { name: 'Dashboard Design', level: 88 },
    ],
  },
  {
    title: 'Tools & Platforms',
    icon: Database,
    skills: [
      { name: 'JIRA', level: 92 },
      { name: 'Azure DevOps', level: 88 },
      { name: 'Confluence', level: 87 },
      { name: 'Figma', level: 80 },
      { name: 'Lucidchart / Miro', level: 85 },
    ],
  },
  {
    title: 'Technologies',
    icon: Code2,
    skills: [
      { name: 'GitHub', level: 85 },
      { name: 'Digital Banking Systems', level: 88 },
      { name: 'SaaS Platforms', level: 90 },
      { name: 'Financial Systems', level: 87 },
    ],
  },
  {
    title: 'Methodologies',
    icon: TrendingUp,
    skills: [
      { name: 'Agile/Scrum', level: 95 },
      { name: 'Waterfall', level: 85 },
      { name: 'SDLC', level: 90 },
      { name: 'Process Optimization', level: 88 },
    ],
  },
]

export default function Skills() {
  return (
    <section id="skills" className="relative py-20 px-4 sm:px-6 lg:px-8 bg-gradient-to-b from-white via-gray-50 to-white overflow-hidden">
      {/* Dynamic Grey Background Elements */}
      <div className="absolute inset-0 pointer-events-none">
        <div className="absolute top-0 right-0 w-96 h-96 bg-gradient-to-bl from-slate-100 to-transparent opacity-40 rounded-full blur-3xl" />
        <div className="absolute bottom-0 left-0 w-96 h-96 bg-gradient-to-tr from-gray-100 to-transparent opacity-40 rounded-full blur-3xl" />
      </div>

      <div className="max-w-7xl mx-auto relative z-10">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="text-4xl sm:text-5xl font-bold text-center mb-4 gradient-text"
        >
          💼 What I Do
        </motion.h2>

        <motion.p
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ delay: 0.2 }}
          className="text-center text-gray-600 text-lg mb-16 max-w-3xl mx-auto"
        >
          A comprehensive skill set spanning business analysis, data analytics, visualization, and agile delivery
        </motion.p>

        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          {skillCategories.map((category, catIndex) => {
            const Icon = category.icon
            return (
              <motion.div
                key={catIndex}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: catIndex * 0.1 }}
                className="relative bg-white border border-gray-200 rounded-xl p-6 hover:border-slate-300 hover:shadow-xl transition-all duration-300 overflow-hidden group"
                whileHover={{ scale: 1.02, y: -3 }}
              >
                {/* Grey gradient overlay on hover */}
                <div className="absolute inset-0 bg-gradient-to-br from-slate-50 via-gray-50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />

                <div className="relative z-10">
                  <div className="flex items-center gap-3 mb-6">
                    <div className="bg-gradient-to-br from-slate-100 to-gray-50 p-3 rounded-lg group-hover:scale-110 transition-transform duration-300">
                      <Icon className="text-slate-700" size={24} />
                    </div>
                    <h3 className="text-xl font-bold text-slate-900">{category.title}</h3>
                  </div>

                  <div className="space-y-4">
                  {category.skills.map((skill, skillIndex) => (
                    <div key={skillIndex}>
                      <div className="flex justify-between mb-2">
                        <span className="text-gray-700 font-medium">{skill.name}</span>
                        <span className="bg-gradient-to-r from-slate-600 to-slate-800 bg-clip-text text-transparent font-semibold">{skill.level}%</span>
                      </div>
                      <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
                        <motion.div
                          className="h-full bg-gradient-to-r from-slate-600 via-slate-700 to-slate-800 rounded-full"
                          initial={{ width: 0 }}
                          whileInView={{ width: `${skill.level}%` }}
                          viewport={{ once: true }}
                          transition={{ duration: 1, delay: skillIndex * 0.1 }}
                        />
                      </div>
                    </div>
                  ))}
                  </div>
                </div>
              </motion.div>
            )
          })}
        </div>

        {/* Core Specialties */}
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="mt-16 relative bg-gradient-to-r from-slate-50 via-gray-50 to-slate-50 border border-gray-200 rounded-xl p-8 overflow-hidden"
        >
          {/* Decorative grey element */}
          <div className="absolute top-0 left-0 w-64 h-64 bg-gradient-to-br from-slate-200 to-transparent opacity-30 rounded-full blur-3xl" />
          <h3 className="text-2xl font-bold text-slate-900 mb-6 text-center relative z-10">🏆 Core Technologies</h3>
          <div className="flex flex-wrap justify-center gap-3 relative z-10">
            {[
              'Excel',
              'SQL',
              'Power BI',
              'Tableau',
              'Python (pandas)',
              'JIRA',
              'Azure DevOps',
              'Confluence',
              'Figma',
              'Miro',
              'Lucidchart',
              'MS Office',
              'UAT',
              'BRD/FSD',
              'Agile/Scrum',
              'Process Mapping',
              'Digital Banking',
              'FATCA/CRS Reporting',
              'GitHub',
            ].map((specialty, index) => (
              <motion.span
                key={index}
                className="px-4 py-2 bg-white border border-gray-300 rounded-full text-slate-700 font-medium hover:bg-gradient-to-r hover:from-slate-700 hover:to-slate-900 hover:text-white hover:border-slate-700 transition-all duration-300 shadow-sm hover:shadow-md"
                whileHover={{ scale: 1.1, y: -2 }}
                initial={{ opacity: 0, scale: 0 }}
                whileInView={{ opacity: 1, scale: 1 }}
                viewport={{ once: true }}
                transition={{ delay: index * 0.03 }}
              >
                {specialty}
              </motion.span>
            ))}
          </div>
        </motion.div>
        </div>
      </div>
    </section>
  )
}
